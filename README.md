# Social-Connect
Social Connect is a photo-sharing social networking service where users can create profiles, upload media, and interact with other users. The backend is designed as a fully RESTful API architecture serving data in JSON format.
